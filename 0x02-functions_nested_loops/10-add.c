#include "main.h"

/**
 * add - add two integer
 * @m: is int to be added to n
 * @n: is int to be added to m
 * Return: the result
 */
int add(int m, int n)
{

	_putchar(m && n);
	return (m + n);
}


