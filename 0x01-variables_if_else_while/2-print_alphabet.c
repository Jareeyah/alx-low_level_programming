#include <stdio.h>

/**
 * main - a program that prints alphabets in lowercase
 *
 * Return: Always 1 success
 */
int main(void)
{
	char k = 'a';

while (k <= 'z')
	{
	putchar(k);
	k++;
	}

	putchar('\n');
return (0);
}

